//no https // no protocol  & no path. just domain
module.exports = {
   images: {
     domains: ['links.papareact.com', 
   'fakestoreapi.com'],
   },
   env: {
     stripe: {
       stripe_public_key: process.env.
 }