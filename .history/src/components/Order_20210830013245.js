const Order = ({ objects }) => {
  const { id, title, price, category, thumbnail, shipping } = product;

  return <div className="relative border rounded-md">
      <div className="flex items-center space-x-10 bg-gray-100 p-5 ">

      </div>
  </div>;
};

export default Order;
