//SSR (Server-side rendering) to fetch all of the products from that fake store API
//With React when you hit google.com, eveything loads in your browser
//With Next 
const ProductFeed = () => {
   return (
      <div>
         
      </div>
   )
}

export default ProductFeed
